/**
 * An array of routes that are used for authentication.
 * These routes will redirect authenticated user to /dashboard
 */
export const authRoutes: string[] = ['/register', '/login', '/forgot-password']
